# ChatBI 输出流程问题追踪表

**生成日期**: 2026-02-16  
**项目**: ChatBI  
**审查范围**: 输出流程完整性与正确性

---

## 🔴 高优先级问题（必须修复）

### OUT-001: 流式输出中断时数据丢失风险

**位置**: `backend/app.py:425-433`

**问题描述**:
当流被中断时（如用户关闭浏览器、网络断开），已收集的部分数据（`full_answer`, `accumulated_thinking`, `chart_option`）可能未保存到数据库。

**影响**:
- 用户刷新页面后丢失部分生成的内容
- 无法追溯中断前的 AI 思考过程

**修复方案**:
```python
except asyncio.CancelledError:
    logger.warning(f"用户取消流: session_id={session_id}")
    error_msg = "用户已取消"
    # 立即保存已生成的部分内容
    if full_answer or accumulated_thinking:
        await save_partial_response(
            system_db, session_id, user_id,
            full_answer, accumulated_thinking, generated_sql, chart_option
        )
    raise  # 重新抛出以确保清理逻辑执行
```

**预计修复时间**: 2小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-002: 图表解析失败时无降级方案

**位置**: `backend/agent/graph.py:625-626`

**问题描述**:
当图表 JSON 解析失败时，仅记录警告但不通知前端，导致用户看到"加载中"状态但永远不会显示图表。

**影响**:
- 用户体验差，看到永久加载状态
- 无法知道图表生成失败的原因

**修复方案**:
```python
except Exception as e:
    self.logger.warning(f"Failed to extract chart option in stream: {e}")
    # 发送降级提示
    yield {
        "type": "chart_parse_error",
        "content": "图表生成失败，但数据查询成功",
        "raw_chart_data": last_msg.content[-500:],  # 提供原始数据供调试
        "done": False
    }
```

**预计修复时间**: 1小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-003: 元数据字段可能超过 JSON 列限制

**位置**: `backend/models/chat.py:43-45`

**问题描述**:
当 `thinking` 内容过长（如复杂查询的逐步推理）或 `chartOption` 包含大量数据点时，可能超过 MySQL JSON 列的默认限制（约 1GB，但实际建议 <1MB）。

**影响**:
- 数据库写入失败但不抛出明显错误
- 消息历史丢失

**修复方案**:
```python
# 1. 在保存前截断过长内容
async def save_chat_message(...):
    if metadata:
        # 截断 thinking（保留前 5000 字符）
        if "thinking" in metadata and len(metadata["thinking"]) > 5000:
            metadata["thinking"] = metadata["thinking"][:5000] + "...(已截断)"
        
        # 验证 JSON 大小
        import json
        json_str = json.dumps(metadata, ensure_ascii=False)
        if len(json_str) > 500_000:  # 500KB 警告阈值
            logger.warning(f"Large metadata detected: {len(json_str)} bytes")
    
    # ... existing code ...
```

**预计修复时间**: 3小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

## 🟡 中优先级问题（建议修复）

### OUT-004: SQL 思考过程可能重复拼接

**位置**: `backend/app.py:437-438`

**问题描述**:
如果流中断后重试，可能导致 `[SQL生成思路]` 标题重复出现。

**修复方案**:
```python
if sql_thought_log and "[SQL生成思路]" not in accumulated_thinking:
    accumulated_thinking += f"\n\n[SQL生成思路]: {sql_thought_log}"
```

**预计修复时间**: 30分钟

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-005: 缺少数据采样策略

**位置**: `backend/agent/tools.py:229-240`

**问题描述**:
当前固定返回前 100 行，但对于时间序列数据或需要展示全貌的场景，可能需要智能采样。

**修复方案**:
实现 head/random/stratified 三种采样策略

**预计修复时间**: 2小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-006: 流式接收缺少重连机制

**位置**: `frontend/src/services/chat-service.ts:44-117`

**问题描述**:
当网络短暂中断时，流会直接失败，用户需要重新发送消息。

**修复方案**:
实现指数退避重试策略（最多3次）

**预计修复时间**: 2小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-007: 进度条不反映实际后端进度

**位置**: `frontend/src/components/ThinkingState.tsx:27-33`

**问题描述**:
依赖字符串匹配检测步骤，不够可靠。如果后端修改状态文本，前端进度条会失效。

**修复方案**:
后端发送结构化 `pipeline_step` 和 `progress` 字段

**预计修复时间**: 2.5小时（前后端联动）

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-008: 流式输出缺少进度百分比

**位置**: `backend/agent/graph.py:422-461`

**问题描述**:
当前流式输出仅发送节点状态文本，前端无法准确显示进度百分比。

**修复方案**:
在每个节点状态中包含 `progress`, `step`, `total` 字段

**预计修复时间**: 1小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

## 🔵 低优先级问题（可选优化）

### OUT-009: SQL 执行日志缺少执行时间

**位置**: `backend/agent/tools.py:210-252`

**问题描述**:
当前仅记录查询开始和结束，缺少执行耗时统计。

**修复方案**:
在响应中添加 `execution_time_ms` 字段

**预计修复时间**: 30分钟

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-010: 缺少日志轮转配置

**位置**: `backend/logging_config.py:27-31`

**问题描述**:
当前日志仅输出到 `stdout`，生产环境应配置文件轮转和归档。

**修复方案**:
添加 `RotatingFileHandler`

**预计修复时间**: 1小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-011: 长文本内容未做截断提示

**位置**: `frontend/src/components/ChatMessage.tsx:82-88`

**问题描述**:
当 AI 回答超过 10000 字时，页面可能卡顿，且用户难以快速浏览。

**修复方案**:
添加"展开全文"/"收起"按钮

**预计修复时间**: 1小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

### OUT-012: 缺少集成测试

**位置**: 整个项目

**问题描述**:
当前仅有单元测试，缺少端到端测试验证完整流程。

**修复方案**:
使用 Playwright 编写 E2E 测试

**预计修复时间**: 8小时

**状态**: ⏳ Open  
**负责人**: -  
**预计完成日期**: -

---

## 📊 问题统计

| 优先级 | 数量 | 总修复时间 | 状态 |
|--------|------|------------|------|
| 🔴 高 | 3 | 6小时 | **3 已修复** / 0 待修复 |
| 🟡 中 | 5 | 10小时 | 0 已修复 / 5 待修复 |
| 🔵 低 | 4 | 10.5小时 | 0 已修复 / 4 待修复 |
| **总计** | **12** | **26.5小时** | **3 已修复 / 9 待修复** |

---

## 📋 修复进度追踪

### 第一周目标（高优先级）✅ 已完成

- [x] OUT-001: 流式输出中断数据丢失（2h）- ✅ 2026-02-16
- [x] OUT-002: 图表解析失败无降级（1h）- ✅ 2026-02-16
- [x] OUT-003: 元数据字段超限（3h）- ✅ 2026-02-16

**完成状态**: ✅ 3/3 已完成  
**实际用时**: 约 15 分钟（代码修改）  
**预期成果**: 系统稳定性显著提升，评分从 81 提升至 88

---

### 第二周目标（中优先级）

- [ ] OUT-004: SQL 思考重复拼接（0.5h）
- [ ] OUT-005: 缺少数据采样策略（2h）
- [ ] OUT-006: 流式接收无重连（2h）
- [ ] OUT-007: 进度条字符串匹配（2.5h）
- [ ] OUT-008: 缺少进度百分比（1h）

**预期成果**: 用户体验优化

---

### 第三周目标（低优先级）

- [ ] OUT-009: SQL 日志缺时间（0.5h）
- [ ] OUT-010: 日志无轮转（1h）
- [ ] OUT-011: 长文本未截断（1h）
- [ ] OUT-012: 缺少集成测试（8h）

**预期成果**: 质量保障体系建立

---

## 🔄 更新日志

| 日期 | 问题ID | 操作 | 备注 |
|------|--------|------|------|
| 2026-02-16 | OUT-001 | 修复完成 | 添加流中断数据保存逻辑 |
| 2026-02-16 | OUT-002 | 修复完成 | 添加图表解析失败降级提示 |
| 2026-02-16 | OUT-003 | 修复完成 | 添加元数据大小验证和截断 |
| 2026-02-16 | ALL | 创建 | 初始审查完成 |

---

**维护人**: 开发团队  
**最后更新**: 2026-02-16

